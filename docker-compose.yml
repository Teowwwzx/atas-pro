services:
  postgres:
    image: pgvector/pgvector:pg15
    container_name: atas_postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-atas_pro}
      POSTGRES_USER: ${POSTGRES_USER:-atas_pro_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-123123}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - atas_network
    ports:
      - "5433:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB" ]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s

  redis:
    image: redis:7-alpine
    container_name: atas_redis
    command: [ "redis-server", "--appendonly", "yes" ]
    volumes:
      - redis_data:/data
    networks:
      - atas_network
    ports:
      - "6379:6379"
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 5s

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: atas_rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - atas_network
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 30s
      timeout: 30s
      retries: 3

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: atas_api
    env_file:
      - .env
    environment:
      # Override infrastructure URLs for Docker network (overrides .env localhost values)
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-atas_pro_user}:${POSTGRES_PASSWORD:-123123}@postgres:5432/${POSTGRES_DB:-atas_pro}
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: amqp://guest:guest@rabbitmq:5672//
      CELERY_RESULT_BACKEND: redis://redis:6379/1
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - atas_network
    restart: unless-stopped

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: atas_worker
    env_file:
      - .env
    environment:
      # Override infrastructure URLs for Docker network (overrides .env localhost values)
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-atas_pro_user}:${POSTGRES_PASSWORD:-123123}@postgres:5432/${POSTGRES_DB:-atas_pro}
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: amqp://guest:guest@rabbitmq:5672//
      CELERY_RESULT_BACKEND: redis://redis:6379/1
    volumes:
      - ./backend:/app
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - atas_network
    restart: unless-stopped
    command: [ "celery", "-A", "app.core.celery_app.celery_app", "worker", "-l", "info", "--concurrency=2" ]

networks:
  atas_network:
    name: atas_network

volumes:
  postgres_data:
  redis_data:
